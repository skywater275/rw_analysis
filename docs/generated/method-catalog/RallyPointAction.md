# RallyPointAction — 方法目录

**源文件**: `03-deobfuscated\com\corrodinggames\rts\game\units\a\RallyPointAction.java`
**方法总数**: 11

---

## `RallyPointAction` — public RallyPointAction()

- **行号**: 18-20 (3 行)
- **返回**: `public`
- **参数**: ``

### 方法体 (前 10 行)
```java
        super("c_1");
    }

    @Override
    public String a() {
        return a.a("gui.actions.setRally.description", new Object[0]);
    }

    @Override
    public String b() {
```

---

## `a` — String a()

- **行号**: 23-25 (3 行)
- **返回**: `String`
- **参数**: ``

### 字符串常量 (语义锚点)
- `"gui.actions.setRally.description"`

### 调用的方法
- `a.a()`

### 方法体 (前 10 行)
```java
        return a.a("gui.actions.setRally.description", new Object[0]);
    }

    @Override
    public String b() {
        return a.a("gui.actions.setRally", new Object[0]);
    }

    @Override
    public int c() {
```

---

## `b` — String b()

- **行号**: 28-30 (3 行)
- **返回**: `String`
- **参数**: ``

### 字符串常量 (语义锚点)
- `"gui.actions.setRally"`

### 调用的方法
- `a.a()`

### 方法体 (前 10 行)
```java
        return a.a("gui.actions.setRally", new Object[0]);
    }

    @Override
    public int c() {
        return 0;
    }

    @Override
    public int b(UnitInstance am2, boolean bl) {
```

---

## `c` — int c()

- **行号**: 33-35 (3 行)
- **返回**: `int`
- **参数**: ``

### 方法体 (前 10 行)
```java
        return 0;
    }

    @Override
    public int b(UnitInstance am2, boolean bl) {
        return -1;
    }

    public ar K() {
        return null;
```

---

## `b` — int b(UnitInstance am2, boolean bl)

- **行号**: 38-40 (3 行)
- **返回**: `int`
- **参数**: `UnitInstance am2, boolean bl`

### 方法体 (前 10 行)
```java
        return -1;
    }

    public ar K() {
        return null;
    }

    @Override
    public u e() {
        return u.d;
```

---

## `K` — ar K()

- **行号**: 42-44 (3 行)
- **返回**: `ar`
- **参数**: ``

### 方法体 (前 10 行)
```java
        return null;
    }

    @Override
    public u e() {
        return u.d;
    }

    @Override
    public t f() {
```

---

## `e` — u e()

- **行号**: 47-49 (3 行)
- **返回**: `u`
- **参数**: ``

### 方法体 (前 10 行)
```java
        return u.d;
    }

    @Override
    public t f() {
        return t.b;
    }

    @Override
    public boolean g() {
```

---

## `f` — t f()

- **行号**: 52-54 (3 行)
- **返回**: `t`
- **参数**: ``

### 方法体 (前 10 行)
```java
        return t.b;
    }

    @Override
    public boolean g() {
        return false;
    }

    @Override
    public e j() {
```

---

## `g` — boolean g()

- **行号**: 57-59 (3 行)
- **返回**: `boolean`
- **参数**: ``

### 方法体 (前 10 行)
```java
        return false;
    }

    @Override
    public e j() {
        return l.B().bS.bj;
    }

    @Override
    public boolean h() {
```

---

## `j` — e j()

- **行号**: 62-64 (3 行)
- **返回**: `e`
- **参数**: ``

### 调用的方法
- `l.B()`

### 方法体 (前 10 行)
```java
        return l.B().bS.bj;
    }

    @Override
    public boolean h() {
        return true;
    }

    @Override
    public /* synthetic */ as i() {
```

---

## `h` — boolean h()

- **行号**: 67-69 (3 行)
- **返回**: `boolean`
- **参数**: ``

### 方法体 (前 10 行)
```java
        return true;
    }

    @Override
    public /* synthetic */ as i() {
        return this.K();
    }
}

```

---
