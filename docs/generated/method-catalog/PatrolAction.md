# PatrolAction — 方法目录

**源文件**: `03-deobfuscated\com\corrodinggames\rts\game\units\a\PatrolAction.java`
**方法总数**: 13

---

## `PatrolAction` — public PatrolAction()

- **行号**: 17-19 (3 行)
- **返回**: `public`
- **参数**: ``

### 方法体 (前 10 行)
```java
        super("c_9");
    }

    @Override
    public int b(UnitInstance am2, boolean bl) {
        return -1;
    }

    @Override
    public int c() {
```

---

## `b` — int b(UnitInstance am2, boolean bl)

- **行号**: 22-24 (3 行)
- **返回**: `int`
- **参数**: `UnitInstance am2, boolean bl`

### 方法体 (前 10 行)
```java
        return -1;
    }

    @Override
    public int c() {
        return 0;
    }

    public ar w() {
        return null;
```

---

## `c` — int c()

- **行号**: 27-29 (3 行)
- **返回**: `int`
- **参数**: ``

### 方法体 (前 10 行)
```java
        return 0;
    }

    public ar w() {
        return null;
    }

    @Override
    public u e() {
        return u.m;
```

---

## `w` — ar w()

- **行号**: 31-33 (3 行)
- **返回**: `ar`
- **参数**: ``

### 方法体 (前 10 行)
```java
        return null;
    }

    @Override
    public u e() {
        return u.m;
    }

    @Override
    public t f() {
```

---

## `e` — u e()

- **行号**: 36-38 (3 行)
- **返回**: `u`
- **参数**: ``

### 方法体 (前 10 行)
```java
        return u.m;
    }

    @Override
    public t f() {
        return t.a;
    }

    @Override
    public boolean g() {
```

---

## `f` — t f()

- **行号**: 41-43 (3 行)
- **返回**: `t`
- **参数**: ``

### 方法体 (前 10 行)
```java
        return t.a;
    }

    @Override
    public boolean g() {
        return false;
    }

    @Override
    public String a() {
```

---

## `g` — boolean g()

- **行号**: 46-48 (3 行)
- **返回**: `boolean`
- **参数**: ``

### 方法体 (前 10 行)
```java
        return false;
    }

    @Override
    public String a() {
        return a.a("gui.actions.patrol.description", new Object[0]);
    }

    @Override
    public String b() {
```

---

## `a` — String a()

- **行号**: 51-53 (3 行)
- **返回**: `String`
- **参数**: ``

### 字符串常量 (语义锚点)
- `"gui.actions.patrol.description"`

### 调用的方法
- `a.a()`

### 方法体 (前 10 行)
```java
        return a.a("gui.actions.patrol.description", new Object[0]);
    }

    @Override
    public String b() {
        return a.a("gui.actions.patrol", new Object[0]);
    }

    @Override
    public boolean s() {
```

---

## `b` — String b()

- **行号**: 56-58 (3 行)
- **返回**: `String`
- **参数**: ``

### 字符串常量 (语义锚点)
- `"gui.actions.patrol"`

### 调用的方法
- `a.a()`

### 方法体 (前 10 行)
```java
        return a.a("gui.actions.patrol", new Object[0]);
    }

    @Override
    public boolean s() {
        return true;
    }

    @Override
    public float l() {
```

---

## `s` — boolean s()

- **行号**: 61-63 (3 行)
- **返回**: `boolean`
- **参数**: ``

### 方法体 (前 10 行)
```java
        return true;
    }

    @Override
    public float l() {
        if (!com.corrodinggames.rts.gameFramework.f.g.bP) {
            return 0.6f;
        }
        return 0.5f;
    }
```

---

## `l` — float l()

- **行号**: 66-71 (6 行)
- **返回**: `float`
- **参数**: ``

### 方法体 (前 10 行)
```java
        if (!com.corrodinggames.rts.gameFramework.f.g.bP) {
            return 0.6f;
        }
        return 0.5f;
    }

    @Override
    public boolean h() {
        return true;
    }
```

---

## `h` — boolean h()

- **行号**: 74-76 (3 行)
- **返回**: `boolean`
- **参数**: ``

### 方法体 (前 10 行)
```java
        return true;
    }

    @Override
    public boolean o_() {
        return true;
    }

    @Override
    public /* synthetic */ as i() {
```

---

## `o_` — boolean o_()

- **行号**: 79-81 (3 行)
- **返回**: `boolean`
- **参数**: ``

### 方法体 (前 10 行)
```java
        return true;
    }

    @Override
    public /* synthetic */ as i() {
        return this.w();
    }
}

```

---
